import React from 'react'

const SectionTitle = props => {
  return (
    <div className="SectionTitle-container">
      <p className="SectionTitle-text">{props.titleName}</p>
    </div>
  )
}

export default SectionTitle
